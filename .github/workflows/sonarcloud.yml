# This workflow is redundant and has been replaced by axiom_zenith_ci.yml
# It is being disabled to prevent 'repository not found' errors from archived actions.
name: Disabled SonarCloud
on:
  push:
    branches: [ non-existent-branch ]
  pull_request:
    branches: [ non-existent-branch ]
jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is disabled."
