name: Valgrind Analysis

on:
  pull_request:
    branches:
      - master

jobs:
  valgrind:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Run Valgrind
        run: valgrind ./build/tests